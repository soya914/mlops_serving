# 붓꽃 분류기 GCP 배포 — 실행 기록

기록 시각: 2026-08-27 15:07 (KST)

- Docker Hub: `soya14/iris-classifier:v1`, `soya14/iris-frontend:v1`
- GCP: 프로젝트 `bold-guru-501604-b2` / 인스턴스 `iris-vm` (us-central1-a, e2-micro)
- 외부 IP: `34.10.33.109`  ← VM 재시작 시마다 바뀜

---

## 1. 로컬(WSL Ubuntu)에서 빌드한 이미지

```
﻿$ docker images

iris-classifier:v1          05e2b82ac138        570MB          133MB        
python:3.13-slim            7e3a6aca9d74        178MB         45.2MB        
soya14/iris-classifier:v1   05e2b82ac138        570MB          133MB        
soya14/iris-frontend:v1     777891321d4b        778MB          179MB
```

`iris-classifier:v1` 과 `soya14/iris-classifier:v1` 의 이미지 ID 가 `05e2b82ac138` 로 동일 —
같은 이미지에 태그만 추가해 푸시한 것.

---

## 2. Docker Hub 등록 결과

| 이미지 | 태그 | Digest | OS/ARCH | 압축 크기 |
|---|---|---|---|---|
| `soya14/iris-classifier` | v1 | `05e2b82ac138` | linux/amd64 | 126.74 MB |
| `soya14/iris-frontend` | v1 | `777891321d4b` | linux/amd64 | — |

로컬 빌드 ID 와 Hub 의 digest 가 일치.

---

## 3. GCP 인스턴스 / 방화벽

```
$ gcloud compute instances list
NAME     ZONE           MACHINE_TYPE  PREEMPTIBLE  INTERNAL_IP  EXTERNAL_IP   STATUS
iris-vm  us-central1-a  e2-micro                   10.128.0.2   34.10.33.109  RUNNING

$ gcloud compute firewall-rules list
NAME                    NETWORK  DIRECTION  ALLOW                         TARGET_TAGS
allow-streamlit-8501    default  INGRESS    tcp:8501                      http-server
default-allow-http      default  INGRESS    tcp:80                        http-server
default-allow-icmp      default  INGRESS    icmp
default-allow-internal  default  INGRESS    tcp:0-65535,udp:0-65535,icmp
default-allow-rdp       default  INGRESS    tcp:3389
default-allow-ssh       default  INGRESS    tcp:22
```

---

## 4. VM 안에서 실행 중인 컨테이너

```
$ sudo docker ps
IMAGE                       STATUS          PORTS
soya14/iris-frontend:v1     Up 11 minutes   0.0.0.0:8501->8501/tcp, :::8501->8501/tcp
soya14/iris-classifier:v1   Up 11 minutes   0.0.0.0:80->8000/tcp, :::80->8000/tcp

$ sudo docker images
REPOSITORY               TAG       SIZE
soya14/iris-frontend     v1        559MB
soya14/iris-classifier   v1        401MB

$ free -m
               total        used        free      shared  buff/cache   available
Mem:             969         639         143           0         329         330
Swap:           1023          11        1012
```

두 컨테이너 모두 `--restart unless-stopped` 가 걸려 있어, VM 을 stop → start 해도
수동 실행 없이 자동으로 올라온다. (재부팅 1회 + stop/start 1회로 검증)

---

## 5. API 실제 호출 결과

`POST http://34.10.33.109/predict`

| 입력 | 응답 |
|---|---|
| `{"data": [5.1, 3.5, 1.4, 0.2]}` | `{"class_index": 0, "class_name": "setosa"}` |
| `{"data": [6.7, 3.0, 5.2, 2.3]}` | `{"class_index": 2, "class_name": "virginica"}` |

Swagger UI 실행 결과: `200`, `server: uvicorn`

---

## 캡처 파일

| 파일 | 내용 | 종류 |
|---|---|---|
| `01_swagger_docs.png` | FastAPI Swagger 문서 | 화면 캡처 |
| `02_swagger_predict_result.png` | POST /predict 실행 결과 | 화면 캡처 |
| `03_streamlit_before.png` | 스트림릿 UI | 화면 캡처 |
| `04_streamlit_result.png` | 예측 결과 | 화면 캡처 |
| `05_dockerhub_iris-classifier.png` | Docker Hub 태그 페이지 | 화면 캡처 |
| `06_dockerhub_iris-frontend.png` | Docker Hub 태그 페이지 | 화면 캡처 |
| `07_cmd_local_build.png` | 로컬 이미지 목록 | **명령어 출력을 이미지로 렌더링** |
| `08_cmd_vm_docker.png` | VM 컨테이너 상태 | **명령어 출력을 이미지로 렌더링** |
| `09_cmd_gcloud.png` | 인스턴스·방화벽 | **명령어 출력을 이미지로 렌더링** |

07~09 는 터미널 화면을 그대로 찍은 것이 아니라, 위 1·3·4 절의 실제 출력 텍스트를
읽기 좋게 렌더링한 것이다. 원문은 이 문서에 그대로 실려 있다.

## 아직 없는 것

GCP 콘솔 웹 화면(인스턴스 목록, 방화벽 규칙 설정 페이지)은 계정 로그인이 필요해
직접 캡처해야 한다. 같은 정보는 3절의 gcloud 출력으로 대체되어 있다.
