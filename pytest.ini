[pytest]
# 레포 루트를 import 경로에 넣는다.
# 이게 없으면 `pytest` 로 실행할 때 tests/ 만 sys.path 에 들어가서
# `from main import app` 이 ModuleNotFoundError 로 죽는다.
# (`python -m pytest` 는 CWD 를 자동으로 넣어줘서 로컬에선 통과해버림 — 그래서 더 헷갈린다)
pythonpath = .
testpaths = tests
